<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 630">
  <!-- Background -->
  <rect width="1200" height="630" fill="#fdf6e3"/>

  <!-- Cute pattern background -->
  <defs>
    <pattern id="dots" width="40" height="40" patternUnits="userSpaceOnUse">
      <circle cx="20" cy="20" r="2" fill="#ff6b9d" opacity="0.2"/>
    </pattern>
  </defs>
  <rect width="1200" height="630" fill="url(#dots)"/>

  <!-- Floating books decoration -->
  <text x="100" y="150" font-size="60" opacity="0.3">📚</text>
  <text x="1050" y="180" font-size="50" opacity="0.3">📖</text>
  <text x="150" y="500" font-size="45" opacity="0.3">📕</text>
  <text x="1000" y="520" font-size="55" opacity="0.3">📗</text>

  <!-- Main card -->
  <g transform="translate(200, 140)">
    <!-- Card shadow -->
    <rect x="10" y="10" width="800" height="350" fill="#2d2d2d"/>
    <!-- Card background -->
    <rect x="0" y="0" width="800" height="350" fill="#fff" stroke="#2d2d2d" stroke-width="6"/>

    <!-- Header bar -->
    <rect x="3" y="3" width="794" height="60" fill="#ff6b9d" opacity="0.2"/>
    <line x1="3" y1="63" x2="797" y2="63" stroke="#2d2d2d" stroke-width="4"/>

    <!-- Title -->
    <text x="400" y="170" text-anchor="middle" font-family="monospace" font-size="72" font-weight="bold" fill="#2d2d2d">
      <tspan fill="#ff6b9d">L</tspan><tspan fill="#7c5cff">e</tspan><tspan fill="#ffd700">n</tspan><tspan fill="#4ade80">d</tspan><tspan fill="#60a5fa">y</tspan>
    </text>

    <!-- Subtitle -->
    <text x="400" y="230" text-anchor="middle" font-family="monospace" font-size="24" fill="#666">
      Your cozy book lending library
    </text>

    <!-- Decorative line -->
    <line x1="250" y1="270" x2="550" y2="270" stroke="#2d2d2d" stroke-width="4" stroke-dasharray="8 8"/>

    <!-- Features -->
    <text x="400" y="310" text-anchor="middle" font-family="monospace" font-size="20" fill="#888">
      📚 Track  •  🤝 Lend  •  📖 Borrow
    </text>
  </g>

  <!-- Sparkles -->
  <text x="180" y="300" font-size="30">✨</text>
  <text x="1000" y="350" font-size="30">⭐</text>
</svg>
